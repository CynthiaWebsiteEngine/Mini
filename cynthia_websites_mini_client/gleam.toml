name = "cynthia_websites_mini_client"
target = "javascript"
gleam = ">= 1.9.0"

[javascript]
typescript_declarations = true

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
plinth = { git = "https://github.com/strawmelonjuice/plinth.git", ref = "master" }
gleam_javascript = ">= 1.0.0 and < 2.0.0"
lustre = ">= 5.0.0 and < 6.0.0"
rsvp = "1.1.0"
cynthia_websites_mini_shared = { path = "../cynthia_websites_mini_shared" }
gleam_fetch = ">= 1.1.0 and < 2.0.0"
modem = ">= 2.0.2 and < 3.0.0"
gleam_time = ">= 1.2.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
birdie = ">= 1.2.7 and < 2.0.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
# description = ""
# licences = ["Apache-2.0"]
# repository = { type = "github", user = "", repo = "" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.
